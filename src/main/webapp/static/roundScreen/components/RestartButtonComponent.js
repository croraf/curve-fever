import React from 'react';
import ReactDOM from 'react-dom';

export default function RestartButtonComponent () {

    if (playerName == "rafa"){
        return  (
                    <div id = "restart" className="controlRowElement">
                            RESTART
                    </div>
                );
    } else {
        return null;
    }
}